import { StyleSheet } from 'react-native'

export const contractDataStyles = StyleSheet.create({
  contractData: {
    flex: 1,
    paddingLeft: 8,
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    gap: 4
  }
})
