import { StyleSheet } from 'react-native'

export const participantDataStyles = StyleSheet.create({
  participantData: {
    flex: 1,
    paddingLeft: 8,
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    gap: 4
  },
  link: {
    marginVertical: 8
  }
})
