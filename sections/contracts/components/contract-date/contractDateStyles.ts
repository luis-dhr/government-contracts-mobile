import { StyleSheet } from 'react-native'

export const contractDateStyles = StyleSheet.create({
  contractDate: {
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center'
  },
  contractDateText: {
    fontSize: 16,
    fontFamily: 'Overpass-Medium'
  }
})
