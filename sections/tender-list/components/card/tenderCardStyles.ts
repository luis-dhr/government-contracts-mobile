import { StyleSheet } from 'react-native'

export const tenderCardStyles = StyleSheet.create({
  tenderCard: {
    width: '100%',
    alignItems: 'flex-start',
    justifyContent: 'center',
    borderRadius: 8,
    padding: 16,
    marginBottom: 8,
    marginTop: 8
  },
  contractTitle: {
    fontSize: 24,
    fontFamily: 'Overpass-Bold'
  },
  contractSubtitle: {
    fontSize: 18,
    fontFamily: 'Overpass-Medium'
  }
})
